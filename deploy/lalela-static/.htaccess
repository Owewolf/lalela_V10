RewriteEngine On

# Let PHP API endpoints handle requests before the SPA catch-all.
RewriteRule ^api/ - [L]

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.html [L]